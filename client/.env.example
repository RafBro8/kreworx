# Copy to .env.local for local development.

# Left unset, the app calls /api and Vite proxies it to the local server.
# In production this points at the Render service, including the /api suffix.
# VITE_API_URL=https://kreworx-api.onrender.com/api
