# Nothing to set. The app always calls /api on its own origin:
#   locally, Vite proxies /api to the server on port 4200;
#   on Vercel, vercel.json rewrites /api to the Render service.
# Keeping the API same-origin is what keeps the session cookie first-party.
