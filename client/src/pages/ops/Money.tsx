import { ComingUp } from "../../components/ui";

export default function Money() {
  return (
    <ComingUp title="Quotes and invoices" stage="Stage 5">
      A quote written on site, sent as a link the customer approves with one tap, and turned
      into an invoice when the work is done. Card payments run in Stripe test mode, and the
      PDF is generated from the same design system as the rest of the product.
    </ComingUp>
  );
}
